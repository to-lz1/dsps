# see: https://golangci-lint.run/usage/configuration/

run:
  tests: true

issues:
  exclude-use-default: false

linters:
  enable:
    - godox
    - goimports
    - gosec
    - nolintlint
    - prealloc
    - revive
    - misspell

